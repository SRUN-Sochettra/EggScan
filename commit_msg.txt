feat: optimize ReadmeRater performance by parallelizing API calls

This PR implements a performance improvement identified for the Bolt persona:
1. Parallelizes independent API calls in `ScanService.rateReadmes` using `CompletableFuture.supplyAsync`.
2. Fetches the profile README concurrently with the user scan and top repos fetch to minimize total latency.
